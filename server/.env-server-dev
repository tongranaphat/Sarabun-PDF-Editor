# ============================================
# Local Development (ไม่ใช้ Docker)
# ใช้ PostgreSQL service ที่รันอยู่ port 5432
# แยกจากโปรเจกต์เก่าด้วยชื่อ DB ต่างกัน
# ============================================

# Database - ใช้ postgres service ปกติ (port 5432)
DATABASE_URL="postgresql://postgres:dbpass@localhost:5432/document_management_db?schema=public"

# Server
PORT=4011

# CORS
CLIENT_URL=http://10.0.0.132:4050
ALLOWED_ORIGINS=http://localhost:5174,http://localhost:4011,http://localhost:4050
